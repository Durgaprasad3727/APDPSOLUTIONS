Client (React) quick start:
1. Create a React app (create-react-app).
2. Copy client/src/firebase.js into your project and fill in Firebase config.
3. Copy components from client/components/ into src/components/.
4. Install firebase: npm install firebase
5. Run app: npm start
